流量包设计
客户属性
你买的是一个流量池（按量计价，可以在不同的物联网卡上共享使用）
里面混合了省内和省漫游的包体（流量组合方式）
整个月有效（流量有效期）

商务属性
这个资源对指定省份有效
#而且限价，
可当做红包使用，也可转移共享，
我们指定该资源是不带票的资源

如果是特殊的通道，先加编码
计费类型（应用范围）：流量池，流量包
比如流量池资源，1日包资源，3日包资源，7日包资源，季度包资源，半年包资源，年包资源，
共享，转移，红包

可以给你配置这样的特殊通道

不考虑混合，包体不变，那怎么筛选出真正的包体
加一个validity有效期字段
用客户属性去筛选包体，用商务属性去设置折扣

通道信息加两个属性
通道类型:共享包通道，红包通道，转移通道，普通通道

业务类型加一个混合，进行筛选

规格加一个筛选：

混合的话，不显示通道规格

添加编码的地方，选定第一个标准包，选定第二个标准包


包体加上包体有效期和包体应用范围字段，添加的时候增加一个包体有效期和包体应用范围筛选
编码同样
通道也同样在异步加载规格的时候，也增加这两个字段
在所有查询唯一编码的地方，也需要添加这两个字段值，否则使用默认的月包和流量包

