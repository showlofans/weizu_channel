传单日志：
下游传单参数，平台生成的订单号，号码，传单结果，时间（传单ip+传单结果描述），传单方向（异常不记录）0

上游返回的数据参数日志，提单流水号，提单号码，提单结果，传单方向,时间（提单ip：异常也要记录）

接口测试，宁可用线上的数据，也不在没有测试的情况下，上传新的代码

订单状态查询和推送日志，查询次数拦截器，超管订单异步提交

传单日志：没有订单号的都是下游异常的传单记录（where purchase_id is null）
对于下游，参数很重要，对于上游，只能查到传入的参数，和自己封装的chargeDTO结果