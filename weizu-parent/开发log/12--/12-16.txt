删除默认绑定的？如果有第二张银行卡，就绑定了，就不需要加载默认的银行卡

删除之后，能把这个绑定删除，同时也有默认的

加载全部，包括绑定了父级代理商默认银行卡父级代理的

如果没有绑定默认的父级代理商，就设置为可绑定的（未绑定的）
添加默认绑定的
如果有绑定的默认银行卡

绑定了默认绑定的。

把默认绑定的更新成不默认的，


不能删除默认的绑定，绑定默认的绑定，删除绑定了默认的绑定的银行卡
解决主动查询状态参数问题
