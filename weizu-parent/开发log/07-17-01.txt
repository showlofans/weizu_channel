编辑折扣，先提示是否确认解除所有相关联的绑定，
确认之后，马上修改该折扣id相关绑定代理商的绑定状态
然后更新该折扣id的折扣

折扣类型不改，只改折扣

添加折扣和修改折扣共用页面：根据传入和返回的参数，确定提交地址和参数

代理商名称，真实姓名（筛选），联系电话，

查询没有绑定该折扣的下级代理商或者，绑定了但是绑定状态为1.

查询绑定了且绑定状态为0的下级代理商
绑定了

筛选没有绑定的和绑定状态为1的

查询下级代理商

费率列表加载绑定状态为绑定的代理商列表（包括代理商搜索也是）

如果代理商绑定了，就更新绑定状态为绑定，如果没绑定费率id，就直接绑定

查询绑定了的代理商，取出来之后与列表匹配，匹配了的，就取出来，更新这部分代理商的绑定状态 为0，update set where agencyId in (ids)

没绑定的，就直接调用绑定方法(批量添加绑定)

折扣和代理商，是一对多的关系，在折扣那里应该有个代理商列表list

list在remove之前要复制一份，因为是引用变量

