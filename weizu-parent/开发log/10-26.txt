通道和通道折扣是一对一的关系，平台和通道是一对多的关系，
当连表查询得到的是所有通道的数目

通过通道id去找到包体列表

如果按照七日包来找，能找到相关的通道吗，
可以：在通道中找到

费率票务不为空：把超管自己的订单排除出去，同时按照费率类型进行搜索
代理商id换成账户id，间接的解决了按照票务类型搜索的问题。
统一成了扣款账户类型搜索

普通流量，月包，流量包 常规添加通道的方式
7日包，流量包，特殊通道
红包流量，月包，流量包，特殊通道

数据库：发现数据库有同样价格，移动，省内，不同流量大小的包体

省漫游，不带票的，移动，可能会有多个，有可能有红包的通道，也可能有普通流量包通道

充值：根据它的通道折扣属性和包体属性，可以筛选出真正的费率，也可以筛选出真正要充值的通道费率折扣，以及该通道绑定的包体有哪些

怎么去获得所有设定了的折扣属性，会得到一个费率列表，
里面有所有的channelType信息，如果只有一个普通通道，那就直接加载这个通道折扣，这个通道绑定的包体，

仅有的参数是归属地信息，业务类型，
方案一：

判断使用状态是使用，并且通道绑定的包体有出现特殊包体，
根据通道id，判定存在那些特殊配置选项
就显示该特殊配置选项

通过通道id，找到所有的特殊配置选项。然后显示出来

有很多个费率，说明就有很多个通道id，在这些通道id的后面隐藏了一些

通过通道id，先找一次，找到异类的通道，比如红包通道，或者通道包体中包含了异类包体的通道

再通过通道id，找到包体id中的异类

异类通道表
通道id
通道类型
包体类型，

通过找到的费率列表中的通道id和所有异类中的通道id做比较
符合的，就把这个异类通道中的特殊类型加上（方便做选择）：设置三个异类枚举

通过通道id找到异类枚举值

前面再传三个参数来的时候，怎么确定通道，和其包体呢


充值最主要是要确定类型，然后是确定包体
确定完了三个类型，就基本上确定了是哪个通道，然后通过通道

把另外两个的枚举值加上就可以了


根据代理商找通道。根据通道找异类枚举（先找通道类型枚举，然后返回，再找包体异类枚举）

通道绑定了8个包体
绑定了一个折扣